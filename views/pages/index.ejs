<%- include("./../partials/header.ejs") %>

    <div class="container">
        <div class="top-text">
            Hello, <%= name %>
        </div>
        <% if (role !="agent" ) { %>
            <div>
                <div class="create">
                    <button class="button" id="openAddFormButton">Create New Ticket</button>
                </div>

                <div class="form-popup" id="form">
                    <form action="/create-ticket" method="POST" class="popup-container">
                        <h1>New Ticket</h1>

                        <label for="title"><b>Title</b></label>
                        <input type="text" placeholder="Title" name="title" value="" required>

                        <label for="description"><b>Description</b></label>
                        <textarea type="text" placeholder="Description" name="description" value="" required></textarea>
                        <button type="submit" class="btn">Add</button>
                        <button type="button" class="btn cancel" id="closeAddForm">Close</button>
                    </form>
                </div>
            </div>
            <% } %>
                <div class="table-container">
                    <table class="styled-table">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Title</th>
                                <th>Description</th>
                                <th>Creator</th>
                                <th>Agent</th>
                                <th>Created</th>
                                <th>Department</th>
                                <th>Status</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% showTickets.forEach(ticket=> { %>
                                <tr>
                                    <td>
                                        <%= ticket.idTickets %>
                                    </td>
                                    <td>
                                        <%= ticket.titel %>
                                    </td>
                                    <td class="description">
                                        <%= ticket.description %>
                                    </td>
                                    <td>
                                        <%= ticket.creator_name %>
                                    </td>
                                    <td>
                                        <%= ticket.agent_id %>
                                    </td>
                                    <td>
                                        <%= ticket.created_datetime %>
                                    </td>
                                    <td>
                                        <%= ticket.department %>
                                    </td>
                                    <td class="
                                        <%= ticket.status === 'Solved' ? 'status-solved' :
                                            ticket.status === 'Closed' ? 'status-closed' :
                                            ticket.status === 'Processed' ? 'status-processed' :
                                            ticket.status === 'Created' ? 'status-created' : 'status-unknown' %>
                                    ">
                                        <%= ticket.status %>
                                    </td>
                                </tr>
                                <% }) %>
                        </tbody>
                    </table>
                </div>
    </div>

    <%- include("./../partials/footer.ejs") %>
